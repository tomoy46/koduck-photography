name: Unpack LUMIWING website ZIP

on:
  push:
    paths:
      - 'lumiwing-photography.zip'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Unpack website
        run: |
          rm -rf .site-tmp
          mkdir .site-tmp
          unzip -q -o lumiwing-photography.zip -d .site-tmp
          cp -a .site-tmp/lumiwing/. ./
          rm -rf .site-tmp lumiwing-photography.zip

      - name: Commit website files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Publish LUMIWING website"
            git push
          fi
